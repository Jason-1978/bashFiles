#!/bin/bash

# This file is loaded from ._bashLoader 'array for loop'
echo "	${BLF}.bashSpecial${CLEAR} loaded successfully"

# System Specific Exports for Linux Distros

echo "                ...but is not used."

# --> moved to .bashExports by Jason --:

#export PATH="$HOME/.bash/.bash_myExecutables:$PATH"


