#!/bin/bash

# This file is loaded from ._bashLoader 'array for loop'
echo "	${BLF}.bashExports${CLEAR} loaded successfully"

# System Specific Exports for my MacBook

echo "                ...but is not used."

# --> moved to .bashExports by Jason --:
#export PATH="$HOME/.bash/.bash_myExecutables/bin:$PATH"
