#!/bin/bash

# This file is loaded from ._bashLoader 'array for loop'
echo "	${BLF}.bashExports${CLEAR} loaded successfully"

#############################################

# Author: Jason Kralik
# Purpose: 
#  
# FileName: .bashExports.sh
# Created: 02/17/19 
# Modifed: 

#############################################

# --> added by Jason to enable my scripts --:
export PATH="$HOME/.bash/.bash_myExecutables:$PATH"
export PATH="/usr/local/nodejs/bin:$PATH"

# Added by Jason to use NVM for Node.js on 3-21-19...
export NVM_DIR="$HOME/.nvm"
  [ -s "/usr/local/opt/nvm/nvm.sh" ] && . "/usr/local/opt/nvm/nvm.sh"  
	# This loads nvm
  [ -s "/usr/local/opt/nvm/etc/bash_completion" ] && . "/usr/local/opt/nvm/etc/bash_completion"  
	# This loads nvm bash_completion
